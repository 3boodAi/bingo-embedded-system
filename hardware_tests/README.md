# Hardware Test Sketches

Standalone Arduino sketches for checking individual peripherals during wiring/debugging.

These are not part of the production ESP32 firmware. The production sketch lives in `../arduino_firmware/`.
